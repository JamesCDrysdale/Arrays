import org.example.WordCollection;
import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class WordCollectionTest {
    private WordCollection myWords;

    @Before
    public void setUp(){
        myWords = new WordCollection();
    }

//    @Test
//    public void canGetLengthOfMyWordsArray(){
//        assertEquals(0, myWords.getWordCount()); // Returns 5 and not 0 are there is space for 5 items
//    }



}
